import styled from 'styled-components';

const Container = styled.div`
  display: flex;
  flex-wrap: nowrap;
  overflow-x: hidden;
  height: 400px;
  gap: 20px;
  padding: 0 10px;
`;

export default Container;
